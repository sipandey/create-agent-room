---
name: Bug report
about: Something in create-agent-room isn't working as documented
title: "[Bug] "
labels: bug
---

**create-agent-room version:** (`create-agent-room --version`, or check your `package.json`/lockfile)
**Node version:** (`node --version`)
**OS:**

**Command run:**

```
create-agent-room init ...
```

**Expected behavior:**

**Actual behavior:**

**Relevant config, if applicable** (redact anything sensitive):
- `.agent-room.json` contents
- `.agent-room/guardrails.json` contents
- `--skill-packs` / `--template-source` / `--org` used, if any

**Additional context:**
